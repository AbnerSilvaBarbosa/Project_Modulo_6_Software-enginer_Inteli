import { StyleSheet, Text, View, Button, Image } from 'react-native';

export default styles = StyleSheet.create({
    base: {
        backgroundColor: '#000',
        width: '100%',
        height: '100%',
    },

    tittle: {
        color: '#fff',
        fontSize: 25,
        alignSelf: 'center',
        textAlign: 'center',
    },

    tinam: {
        color: '#8A31E3'
    },

    count: {
        color: 'gray',
        fontSize: 22,
        alignSelf: 'center',
        textAlign: 'center',
    },

    tagAlign: {
        flexDirection: 'row',
        alignSelf: 'center',
        gap: 30,
    },

    align: {
        marginBottom: 20,
    }
});